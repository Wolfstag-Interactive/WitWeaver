name: Build WitWeaver Docs Site

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Checkout repository
      - name: Checkout
        uses: actions/checkout@v4

      # Install Doxygen, Graphviz, rsync
      - name: Install Doxygen and tools
        run: |
          sudo apt-get update
          sudo apt-get install -y doxygen graphviz rsync

      # Generate Doxygen documentation
      - name: Build Doxygen API
        run: doxygen Doxyfile

      # Copy Doxygen output into Docusaurus static folder
      - name: Copy Doxygen HTML into Docusaurus static path
        run: |
          rm -rf website_docusaurus/static/witweaver/api
          mkdir -p website_docusaurus/static/witweaver/api
          rsync -a Docs/html/ website_docusaurus/static/witweaver/api/
      # Generate API sitemap.xml from all Doxygen HTML pages so AI crawlers
      # can discover every class and namespace page without running JavaScript.
      # Written directly into the already-rsynced static folder so it is
      # included in the Docusaurus build regardless of Doxygen version.
      - name: Generate API sitemap.xml
        run: |
          BASE="https://docs.wolfstaginteractive.com/witweaver/api"
          OUT="website_docusaurus/static/witweaver/api/sitemap.xml"
          echo '<?xml version="1.0" encoding="UTF-8"?>' > "$OUT"
          echo '<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">' >> "$OUT"
          find website_docusaurus/static/witweaver/api -maxdepth 1 -name "*.html" | sort | while read f; do
            page=$(basename "$f")
            echo "  <url><loc>$BASE/$page</loc></url>" >> "$OUT"
          done
          echo '</urlset>' >> "$OUT"
          echo "Generated $(grep -c '<url>' "$OUT") URLs in $OUT"

      - name: Ensure copied API contains class pages
        run: |
          test -f website_docusaurus/static/witweaver/api/annotated.html
          test -f website_docusaurus/static/witweaver/api/classes.html

      - name: Ensure Doxygen produced class pages
        run: |
          test -f Docs/html/annotated.html
          test -f Docs/html/classes.html

      # Hard fail if API did not generate correctly
      - name: Ensure API index exists
        run: test -f website_docusaurus/static/witweaver/api/index.html

      # Build Docusaurus site
      - name: Build Docusaurus
        working-directory: website_docusaurus
        run: |
          npm ci
          npm run build

      # Add CNAME for custom domain
      - name: Add CNAME for custom domain
        run: echo "docs.wolfstaginteractive.com" > website_docusaurus/build/CNAME

      # Deploy to gh-pages
      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: website_docusaurus/build
          publish_branch: gh-pages